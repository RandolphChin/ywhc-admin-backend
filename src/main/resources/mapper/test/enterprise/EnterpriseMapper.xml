<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ywhc.admin.modules.test.enterprise.mapper.EnterpriseMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.ywhc.admin.modules.test.enterprise.entity.Enterprise">
        <id column="id" property="id" />
        <result column="enterprise_name" property="enterpriseName" />
        <result column="enterprise_address" property="enterpriseAddress" />
        <result column="dept_id" property="deptId" />
        <result column="status" property="status" />
        <result column="deleted" property="deleted" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="create_by" property="createBy" />
        <result column="update_by" property="updateBy" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        enterprise_name,
        enterprise_address,
        dept_id,
        status,
        deleted,
        create_time,
        update_time,
        create_by,
        update_by
    </sql>

    <!-- 分页查询 -->
    <select id="pageJoin" resultType="com.ywhc.admin.modules.test.enterprise.vo.EnterpriseVO">
        SELECT
            biz_enterprise.*,
            creator.username AS create_by_name,
            updater.username AS update_by_name
        FROM biz_enterprise biz_enterprise
        LEFT JOIN sys_user creator ON creator.id = biz_enterprise.create_by
        LEFT JOIN sys_user updater ON updater.id = biz_enterprise.update_by
        ${ew.customSqlSegment}
    </select>

    <select id="listJoin" resultType="com.ywhc.admin.modules.test.enterprise.vo.EnterpriseVO">
        SELECT
            biz_enterprise.*,
            creator.username AS create_by_name,
            updater.username AS update_by_name
        FROM biz_enterprise biz_enterprise
        LEFT JOIN sys_user creator ON creator.id = biz_enterprise.create_by
        LEFT JOIN sys_user updater ON updater.id = biz_enterprise.update_by
        ${ew.customSqlSegment}
    </select>
</mapper>
